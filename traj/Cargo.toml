[package]
name = "aevia-trajectory"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
license = "Proprietary"
description = "Continuous trajectory and measurement engine for Aevia V2 Mini"
readme = "README.md"

[features]
default = []
offline = ["nalgebra/std"]
gtsam-system = ["offline"]
gtsam-vendored = ["offline"]
raw-tight = ["offline"]

[dependencies]
fpmath = { version = "=0.1.1", default-features = false }
heapless = { version = "=0.9.3", default-features = false }
libm = { version = "=0.2.16", default-features = false }
nalgebra = { version = "=0.34.1", default-features = false, features = ["libm-force"] }
sha2 = { version = "=0.10.9", default-features = false, features = ["force-soft-compact"] }

[dev-dependencies]
# Software arithmetic remains an independent host-test adapter. Production
# enclosures use the allocator-free native backend with outward expansion.
fpmath = { version = "=0.1.1", default-features = false, features = ["soft-float"] }

[lints.rust]
unsafe_code = "deny"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("espidf"))'] }

[lints.clippy]
all = "warn"
pedantic = "warn"
